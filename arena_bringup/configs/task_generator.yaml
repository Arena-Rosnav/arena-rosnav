episodes: -1

map:
  algorithm: none
  config:
    dir: arena_hospital_small
    prompt: 5
  timeout: 60

SCENARIO:
  file: default.json

PARAMETRIZED:
  file: default.xml 

STAGED:
  curriculum: "default.yaml"
  index: 0

RANDOM:
  seed: -1
  static:
    min: 0
    max: 0
    models: ["shelf"]
  interactive:
    min: 0
    max: 0
    models: ["shelf"]
  dynamic:
    min: 5
    max: 8
    models: ["actor1"]

ZONES:
  file: "zones.yaml"
  scenario: "default.yaml"

DYNAMICMAP:
  episodes: 1