episodes: 50
autoreset: true

SCENARIO:
  file: default.json

PARAMETRIZED:
  file: default.xml 

STAGED:
  curriculum: "default.yaml"
  index: 0

DYNAMICMAP: {}

RANDOM:
  static:
    min: 0
    max: 0
    models: ["shelf"]
  interactive:
    min: 0
    max: 0
    models: ["shelf"]
  dynamic:
    min: 1
    max: 5
    models: ["actor1"]